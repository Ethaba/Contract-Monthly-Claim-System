<Window x:Class="CMCS.Views.CoordinatorDashboard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Programme Coordinator Dashboard" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#f9f9fb">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Programme Coordinator Dashboard"
                   FontSize="24" FontWeight="Bold"
                   Foreground="#2a4365" Margin="0,0,0,20"/>

        <!-- Claims Table -->
        <DataGrid Grid.Row="1" Margin="0,0,0,20"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  AlternatingRowBackground="#edf2f7"
                  BorderBrush="#cbd5e0"
                  BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Claim ID" Width="100" Binding="{Binding ClaimId}"/>
                <DataGridTextColumn Header="Lecturer" Width="200" Binding="{Binding Lecturer}"/>
                <DataGridTextColumn Header="Month" Width="150" Binding="{Binding Month}"/>
                <DataGridTextColumn Header="Total Hours" Width="150" Binding="{Binding TotalHours}"/>
                <DataGridTextColumn Header="Amount" Width="150" Binding="{Binding Amount}"/>
                <DataGridTextColumn Header="Status" Width="150" Binding="{Binding Status}"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Verify Claim" Width="140" Height="40"
                    Background="#2b6cb0" Foreground="White"
                    FontWeight="SemiBold" Margin="5"/>
            <Button Content="Request Revision" Width="140" Height="40"
                    Background="#e53e3e" Foreground="White"
                    FontWeight="SemiBold" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
