<Page x:Class="CMCS.Views.ClaimDetailsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Claim Details" Background="#f9f9fb">
    <Grid Margin="12">
        <StackPanel>
            <TextBlock x:Name="HeaderText" FontSize="20" FontWeight="Bold" Foreground="#2a4365"/>
            <TextBlock x:Name="MetaText" Margin="0,6,0,12"/>
            <DataGrid x:Name="ItemsGrid" AutoGenerateColumns="False" CanUserAddRows="False" Height="200">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}"/>
                    <DataGridTextColumn Header="Hours" Binding="{Binding HoursWorked, StringFormat=N2}"/>
                    <DataGridTextColumn Header="Rate" Binding="{Binding HourlyRate, StringFormat=N2}"/>
                    <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat=N2}"/>
                </DataGrid.Columns>
            </DataGrid>

            <TextBlock Text="Supporting Documents" FontWeight="Bold" Margin="0,12,0,6"/>
            <ListBox x:Name="DocsList" Height="120"/>

            <Button Content="Back" Width="120" Margin="0,12,0,0" Click="Back_Click"/>
        </StackPanel>
    </Grid>
</Page>
