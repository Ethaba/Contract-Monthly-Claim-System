@model IEnumerable<CMCS.Web.Models.Claim>
@{
    ViewData["Title"] = "Manager Dashboard";
    var userId = ViewBag.UserId ?? 0;
}

<h2>Academic Manager Dashboard</h2>
<p>Claims awaiting manager decision:</p>

<table class="table table-striped">
    <thead><tr><th>Claim ID</th><th>Lecturer</th><th>Month</th><th>Total</th><th>Status</th><th>Actions</th></tr></thead>
    <tbody>
        @foreach (var c in Model)
        {
            <tr>
                <td>@c.ClaimId</td>
                <td>@c.UserId</td>
                <td>@c.ClaimMonth/@c.ClaimYear</td>
                <td>@c.TotalAmount.ToString("N2")</td>
                <td>@c.Status</td>
                <td>
                    <a asp-controller="Claim" asp-action="Details" asp-route-id="@c.ClaimId" asp-route-userId="@ViewBag.UserId" class="btn btn-sm btn-info">View</a>


                    @* Only allow manager actions when status == Reviewed *@
                    @if (string.Equals(c.Status, "Reviewed", StringComparison.OrdinalIgnoreCase))
                    {
                        <form asp-controller="Claim" asp-action="Approve" method="post" style="display:inline-block; margin-left:5px">
                            <input type="hidden" name="id" value="@c.ClaimId" />
                            <input type="hidden" name="approverId" value="@userId" />
                            <input type="hidden" name="role" value="Manager" />
                            <input type="hidden" name="decision" value="Approved" />
                            <input type="hidden" name="comments" value="Approved by manager" />
                            <button type="submit" class="btn btn-sm btn-success">Approve</button>
                        </form>

                        <form asp-controller="Claim" asp-action="Approve" method="post" style="display:inline-block; margin-left:5px">
                            <input type="hidden" name="id" value="@c.ClaimId" />
                            <input type="hidden" name="approverId" value="@userId" />
                            <input type="hidden" name="role" value="Manager" />
                            <input type="hidden" name="decision" value="Rejected" />
                            <input type="hidden" name="comments" value="Rejected by manager" />
                            <button type="submit" class="btn btn-sm btn-danger">Reject</button>
                        </form>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
